Run the program from main.py
You should be able to run it from the command line but if you run into trouble try installing PyCharm

Additionally, you may need to install some python packages.
Some are :

pip install mediapipe
pip install opencv-python
pip install customtkinter